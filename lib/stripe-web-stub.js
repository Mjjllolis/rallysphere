// Stub file for @stripe/stripe-react-native on web
// On web, we use @stripe/stripe-js instead

// These are just stubs to prevent errors if accidentally imported
export const StripeProvider = () => null;
export const useStripe = () => ({});
export const usePaymentSheet = () => ({});
export const CardField = () => null;

export default {
  StripeProvider,
  useStripe,
  usePaymentSheet,
  CardField,
};
