{
  "hosting": {
    "public": "dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "/event/**",
        "function": "eventPreview"
      },
      {
        "source": "/child-safety",
        "destination": "/child-safety.html"
      },
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "source": "/checkout/tokenize.html",
        "headers": [
          { "key": "Cache-Control", "value": "no-cache, no-store, must-revalidate" }
        ]
      },
      {
        "source": "/.well-known/apple-app-site-association",
        "headers": [
          { "key": "Content-Type", "value": "application/json" }
        ]
      },
      {
        "source": "/.well-known/assetlinks.json",
        "headers": [
          { "key": "Content-Type", "value": "application/json" }
        ]
      }
    ],
    "predeploy": [
      "npx expo export --platform web --output-dir dist",
      "mkdir -p dist/checkout && cp public/checkout/tokenize.html dist/checkout/tokenize.html",
      "if [ -f public/fees.html ]; then cp public/fees.html dist/fees.html; fi",
      "if [ -f public/child-safety.html ]; then cp public/child-safety.html dist/child-safety.html; fi",
      "if [ -f public/og-default.png ]; then cp public/og-default.png dist/og-default.png; fi",
      "if [ -f public/.well-known/apple-developer-merchantid-domain-association ]; then mkdir -p dist/.well-known && cp public/.well-known/apple-developer-merchantid-domain-association dist/.well-known/apple-developer-merchantid-domain-association; fi",
      "if [ -f public/.well-known/apple-app-site-association ]; then mkdir -p dist/.well-known && cp public/.well-known/apple-app-site-association dist/.well-known/apple-app-site-association; fi",
      "if [ -f public/.well-known/assetlinks.json ]; then mkdir -p dist/.well-known && cp public/.well-known/assetlinks.json dist/.well-known/assetlinks.json; fi"
    ]
  },
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "ios",
        "android",
        ".expo",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ],
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run build"
      ]
    }
  ],
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
